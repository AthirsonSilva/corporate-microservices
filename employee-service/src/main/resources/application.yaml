spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/employees
      database: employees

management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      enabled: true
      show-details: always
  info:
    env:
      enabled: true

server:
  port: 8081