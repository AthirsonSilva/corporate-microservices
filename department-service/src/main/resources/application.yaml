spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/departments
      database: departments

management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      enabled: true
      show-details: always
  info:
    env:
      enabled: true

server:
  port: 8080